CREATE SEQUENCE ReceiptIdSeq START WITH 1 INCREMENT BY 1;

CREATE TABLE Receipt (
    ID BIGINT DEFAULT nextval('ReceiptIdSeq') PRIMARY KEY,
    Date DATE NOT NULL,
    Shopname VARCHAR(255) NOT NULL,
    OwnerID INT NOT NULL REFERENCES Users(ID)
);